form(name="form" class="css-form" novalidate, ng-controller="newAssetCtrl")
  ul.libs-js
    li(ng-repeat="lib in libs")
      label
        input(type="checkbox", value="{{lib._id}}")
        {{lib.name}}

  ul.libs-css
    li(ng-repeat="lib in libs")
      label
        input(type="checkbox", value="{{lib._id}}")
        {{lib.name}}
